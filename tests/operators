main() {
    int a = 1;
    int b = 2;
    int x = 0;

    x = a + b;
    x = a - b;
    x = a * b;
    x = a / b;
    x = a <= b;
    x = a >= b;
    x = a == b;
    x = a != b;
    x = a && b;
    x = a || b;
    x = !(a || b);
}
