// This is the recursive bisection algorithm. This should parse.
bisect (l, r) {
    var mid = (r - l) / 2;
    
    if (mid >= E) {
        if (f(r) * f(mid) <= 0) {
            var l = mid;
        } else {
            var r = mid;
        };
        bisect(l, r);
    } else {};

    return $mid;
}
